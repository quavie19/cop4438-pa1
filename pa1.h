#include <stdio.h>
#include <string.h>
#define MAX_LENGTH 1000
#define LIMIT 10

int is_singleton(char str[]);
int is_arithmetic(char str[]);
int is_reverseArithmetic(char str[]);
int is_balancedTripartite(char str[]);
int is_balancedBipartite(char str[]);
int is_palindrome(char str[]);
